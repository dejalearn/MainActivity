<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="fill_parent" android:background="#ECECEC"
    android:layout_height="fill_parent">
 
    <WebView android:id="@+id/img" 
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
         />

    <TextView
        android:id="@+id/question"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="What is the definition of abrogate?"
        android:textColor="#153450"
        android:textStyle="bold"
        android:layout_below="@+id/img"
        android:padding="10px"
        android:textSize="20dp" />
    


    <EditText
        android:id="@+id/userAnswer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="72dp"
        android:ems="10"
        android:layout_below="@+id/question"
        android:inputType="textPersonName" />

    <Button
        android:id="@+id/submitButton"
        android:layout_width="164dp"
        android:layout_height="wrap_content"
        android:background="@drawable/custom_button"
        android:gravity="center"
        android:onClick="checkAnswer"
        android:paddingRight="10dp"
        android:layout_centerInParent="true"
        android:text="Submit"
        android:layout_below="@+id/userAnswer"
        android:textColor="@drawable/custom_button_textcolor" />
        <requestFocus />

     
</RelativeLayout>     